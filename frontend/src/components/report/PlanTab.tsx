/**
 * Stub -- replaced in Task 13, which builds the real Plan tab (steps in
 * order, mitigations, `human_decisions_applied`, `unaddressed_with_reason`,
 * and the validation report). Exists only so `ReportView`'s five-tab shell
 * has something to mount in this task.
 */

import type { FinalReport } from "../../api/types";

export function PlanTab(_props: { report: FinalReport }) {
  return null;
}

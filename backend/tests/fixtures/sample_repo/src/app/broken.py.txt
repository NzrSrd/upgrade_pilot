# pydantic: this file is deliberately unparseable so phase A's byte prefilter
# finds it (this comment names the import root) but `_parse` must still
# record it as a SkippedFile rather than raising.
def broken(
    this file is intentionally not valid Python

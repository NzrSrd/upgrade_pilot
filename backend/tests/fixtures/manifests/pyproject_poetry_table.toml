[tool.poetry]
name = "sample-app"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.11"
pydantic = { version = "^1.10", extras = ["email"] }
requests = { git = "https://github.com/example/requests", branch = "main" }
